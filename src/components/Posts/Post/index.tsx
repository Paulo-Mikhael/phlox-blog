import { PostCard, PostFormatButton, PostPagination, PostRoot, PostCards } from "./PostPattern";

export const Post = {
  Root: PostRoot,
  Card: PostCard,
  FormatButton: PostFormatButton,
  Pagination: PostPagination,
  Cards: PostCards
}