import { FilterDate, FilterRoot, FilterSearchPost, FilterSearchUser } from "./FilterPattern";

export const Filter = {
  Root: FilterRoot,
  SearchPost: FilterSearchPost,
  SearchUser: FilterSearchUser,
  Date: FilterDate
}