import { FormRoot, FormLabel, FormInput, FormHint, FormInputIcon } from "./FormPattern";

export const Form = {
  Root: FormRoot,
  Label: FormLabel,
  Input: FormInput,
  Hint: FormHint,
  InputIcon: FormInputIcon
}